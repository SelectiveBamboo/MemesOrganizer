<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/add_keywords"
        android:icon="@drawable/ic_add_keywords"
        android:title="@string/add_keywords_pic"
        app:showAsAction="ifRoom"
        android:onClick="addKeywordsFromBrowserFragment"></item>

    <item android:id="@+id/share_pic"
        android:icon="@drawable/ic_share"
        android:title="@string/share_picture"
        app:showAsAction="ifRoom"
        android:onClick="sharePicture"></item>

</menu>